---
interface Props {
	sectionClass?: string
}

const { sectionClass } = Astro.props
const classSection = sectionClass ? sectionClass : ""
---

<section class={`relative ${classSection}`}>
	<div class="relative mx-auto max-w-6xl">
		<slot />
	</div>

	<slot name="decotations" />
</section>
